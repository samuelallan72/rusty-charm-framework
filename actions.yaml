echo-params:
  description: run a test
  params:
    string:
      type: string
      description: string param with no default
    string-with-default:
      type: string
      description: example param
      default: something
    fail:
      type: boolean
      description: set action failure state with a message if true
      default: false
log:
  description: test action logging
