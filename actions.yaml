echo-params:
  description: run a test
  params:
    string:
      type: string
      description: string param with no default
    string-with-default:
      type: string
      description: example param
      default: something
    bool:
      type: boolean
      description: example boolean variable, defaulting to false
      default: false
log:
  description: test action logging
